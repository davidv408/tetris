import { UserMechanics } from './user-mechanics';

describe('UserMechanics', () => {
  it('should create an instance', () => {
    const directive = new UserMechanics();
    expect(directive).toBeTruthy();
  });
});
